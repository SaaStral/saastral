{
  "name": "@saastral/cloud",
  "version": "0.1.0",
  "private": true,
  "description": "SaaStral Cloud - Billing, plan enforcement, and cloud-only features (private package)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@saastral/core": "workspace:*",
    "@saastral/database": "workspace:*",
    "@saastral/infrastructure": "workspace:*",
    "@trpc/server": "11.8.1",
    "stripe": "^17.5.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.1.1"
  }
}
