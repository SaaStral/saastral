// Core layer test setup
// No database dependencies - just global utilities for unit tests

import { vi } from 'vitest'

// Configure vi globally if needed
// Example: extend expect matchers, set global test timeout, etc.
